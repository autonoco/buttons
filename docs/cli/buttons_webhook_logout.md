---
title: "buttons webhook logout"
description: "CLI reference for buttons webhook logout"
---

## buttons webhook logout

Clear named-tunnel config (falls back to quick mode)

```
buttons webhook logout [flags]
```

### Options

```
  -h, --help   help for logout
```

### Options inherited from parent commands

```
      --json       output in JSON format
      --no-input   disable all interactive prompts
      --summary    show a read-only plan/snapshot instead of mutating
```

### SEE ALSO

* [buttons webhook](buttons_webhook.md)	 - Expose a local URL via Cloudflare to receive webhook callbacks

