---
title: "buttons flow init"
description: "CLI reference for buttons flow init"
---

## buttons flow init

Register triggers and prove a flow board presses

```
buttons flow init NAME [flags]
```

### Options

```
  -h, --help             help for init
      --manager string   manager agent ref
      --no-verify        skip proof press
      --on string        provider: local|github (default "local")
      --worker string    worker agent ref
```

### Options inherited from parent commands

```
      --json       output in JSON format
      --no-input   disable all interactive prompts
      --summary    show a read-only plan/snapshot instead of mutating
```

### SEE ALSO

* [buttons flow](buttons_flow.md)	 - Buttons Flow — pressable boards for drawer_kind:flow

